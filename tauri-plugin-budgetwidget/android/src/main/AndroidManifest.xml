<?xml version="1.0" encoding="utf-8"?>
<!-- These components merge into the host app's manifest, so the launcher
     discovers the widget under the app package (com.luminaapps.talea). -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <receiver
            android:name="com.luminaapps.talea.budgetwidget.BudgetWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/budget_widget_info" />
        </receiver>

        <!-- BIND_APPWIDGET restricts the caller to the system launcher, closing a
             confused-deputy path where another app could reassign a widget's
             account. The launcher holds this permission; the user flow is
             unaffected. -->
        <activity
            android:name="com.luminaapps.talea.budgetwidget.BudgetWidgetConfigActivity"
            android:exported="true"
            android:permission="android.permission.BIND_APPWIDGET"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>
    </application>
</manifest>
